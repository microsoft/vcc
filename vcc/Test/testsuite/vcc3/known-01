#include <vcc.h>

void foo() {
int i = 0;
do { } while(_(known  \false)i);
} 

void bar() {
int i = 1;
do { } while(_(known  \false)i);
} 
`
Verification of foo succeeded.
Verification of bar failed.
testcase(10,30) : error VC8533: Assertion 'i has the value \false specified by _(known ...)' did not verify.
`
