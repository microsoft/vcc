#include <vcc.h>
void foo(int i) {
  switch (i)
  {
    if (i) i++;
  }
}
`
testcase(5,5) : error VC0000: The keyword case or default must precede code in switch block.
testcase(6,3) : error VC0000: Empty switch block.
`
